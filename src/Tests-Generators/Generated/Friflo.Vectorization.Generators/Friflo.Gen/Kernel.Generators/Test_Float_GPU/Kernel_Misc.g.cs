// <auto-generated/>
using System;
using System.ComponentModel;
using System.Numerics;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.Intrinsics;
using System.Runtime.Intrinsics.X86;
using Friflo.Vectorization.Intrinsics;
using Friflo.Vectorization.GPU;
using Friflo.Vectorization.GPU.Runtime;
using Friflo.Vectorization.WebGPU;
using Friflo.Vectorization.WebGPU.Runtime;


namespace Kernel.Generators
{
    public partial class Test_Float_GPU
    {
        /// <summary>Kernel method generated by: <see cref="Kernel_Misc"/>.</summary>
        public static void Kernel_MiscKernel(
            Buffer<float> position,
          InBuffer<float> velocity,
            float value,
            ComputeMode mode = ComputeMode.Device)
        {
            var buffers =
            GpuBuffers.Create(position, nameof(position), mode);
            buffers.Validate (velocity, nameof(velocity));

            if (buffers.ComputeGPU) {
                _Kernel_Misc_GPU(buffers, position, velocity, value);
                return;
            }
            Kernel_MiscVector(position.Span, velocity.Span, value, buffers.ComputeSIMD);
        }

        /// <summary>Vector method generated for: <see cref="Kernel_Misc"/>.</summary>
        public static void Kernel_MiscVector(Span<float> position, ReadOnlySpan<float> velocity, float value, bool vectorized = true)
        {
            int count = position.Length;
            int n = 0;
            if (vectorized) {
                if (Avx.IsSupported) {
                    n = _Kernel_Misc_Avx(count, position, velocity, value);
                }
            }
            for (; n < count; n++) {
                Kernel_Misc(ref position[n], velocity[n], value);
            }
        }

    #region private members

        // [Layout: AoS-Vertical]  - lane-native speed
        [SkipLocalsInit]
        private static unsafe int _Kernel_Misc_Avx(int count,
            Span<float> position,
            ReadOnlySpan<float> velocity,
            float value)
        {
            int i = 0;
            count -= 32;
            if (i > count) {
                return 0;
            }
            if (position.Length < count) VectorUtils.ThrowBufferTooSmall(nameof(position));
            if (velocity.Length < count) VectorUtils.ThrowBufferTooSmall(nameof(velocity));

            // --- Locals
            var value_scalar = Vector256.Create(value);

            var const0 = Vector256.Create(0x7FFFFFFF).AsSingle(); // Abs()

            fixed (float* position_first = position)
            fixed (float* velocity_first = velocity)
            {
                float* position_ptr = (float*)position_first;
                float* velocity_ptr = (float*)velocity_first;

                for (; i <= count; i += 32)
                {
                    // --- 1. Load
                    Vector256<float> position_0;  // Single
                    Vector256<float> position_1;  // Single
                    Vector256<float> position_2;  // Single
                    Vector256<float> position_3;  // Single

                    Vector256<float> velocity_0 = Avx.LoadVector256(velocity_ptr +  0);  // Single
                    Vector256<float> velocity_1 = Avx.LoadVector256(velocity_ptr +  8);  // Single
                    Vector256<float> velocity_2 = Avx.LoadVector256(velocity_ptr + 16);  // Single
                    Vector256<float> velocity_3 = Avx.LoadVector256(velocity_ptr + 24);  // Single

                    // --- 2. Compute
                    // var abs = MathF.Abs(velocity);
                    var abs_0 = Avx.And(velocity_0, const0);
                    var abs_1 = Avx.And(velocity_1, const0);
                    var abs_2 = Avx.And(velocity_2, const0);
                    var abs_3 = Avx.And(velocity_3, const0);

                    // var sign = MathF.Sign(velocity);
                    var sign_0 = MathUtils.SignMathF(velocity_0);
                    var sign_1 = MathUtils.SignMathF(velocity_1);
                    var sign_2 = MathUtils.SignMathF(velocity_2);
                    var sign_3 = MathUtils.SignMathF(velocity_3);

                    // var floor = MathF.Floor(velocity);
                    var floor_0 = Vector256.Floor(velocity_0);
                    var floor_1 = Vector256.Floor(velocity_1);
                    var floor_2 = Vector256.Floor(velocity_2);
                    var floor_3 = Vector256.Floor(velocity_3);

                    // var ceiling = MathF.Ceiling(velocity);
                    var ceiling_0 = Vector256.Ceiling(velocity_0);
                    var ceiling_1 = Vector256.Ceiling(velocity_1);
                    var ceiling_2 = Vector256.Ceiling(velocity_2);
                    var ceiling_3 = Vector256.Ceiling(velocity_3);

                    // var log = MathF.Log(value);
                    var log_0 = Vector256.Log(value_scalar);
                    var log_1 = Vector256.Log(value_scalar);
                    var log_2 = Vector256.Log(value_scalar);
                    var log_3 = Vector256.Log(value_scalar);

                    // var log2 = MathF.Log2(value);
                    var log2_0 = Vector256.Log2(value_scalar);
                    var log2_1 = Vector256.Log2(value_scalar);
                    var log2_2 = Vector256.Log2(value_scalar);
                    var log2_3 = Vector256.Log2(value_scalar);

                    // var log10 = MathF.Log10(abs);
                    var log10_0 = MathUtils.Log10MathF(abs_0);
                    var log10_1 = MathUtils.Log10MathF(abs_1);
                    var log10_2 = MathUtils.Log10MathF(abs_2);
                    var log10_3 = MathUtils.Log10MathF(abs_3);

                    // var exp = MathF.Exp(velocity);
                    var exp_0 = Vector256.Exp(velocity_0);
                    var exp_1 = Vector256.Exp(velocity_1);
                    var exp_2 = Vector256.Exp(velocity_2);
                    var exp_3 = Vector256.Exp(velocity_3);

                    // var pow = MathF.Pow(abs, velocity);
                    var pow_0 = MathUtils.PowMathF(abs_0, velocity_0);
                    var pow_1 = MathUtils.PowMathF(abs_1, velocity_1);
                    var pow_2 = MathUtils.PowMathF(abs_2, velocity_2);
                    var pow_3 = MathUtils.PowMathF(abs_3, velocity_3);

                    // var round = MathF.Round(velocity);
                    var round_0 = Vector256.Round(velocity_0);
                    var round_1 = Vector256.Round(velocity_1);
                    var round_2 = Vector256.Round(velocity_2);
                    var round_3 = Vector256.Round(velocity_3);

                    // var sqrt = MathF.Sqrt(abs);
                    var sqrt_0 = Avx.Sqrt(abs_0);
                    var sqrt_1 = Avx.Sqrt(abs_1);
                    var sqrt_2 = Avx.Sqrt(abs_2);
                    var sqrt_3 = Avx.Sqrt(abs_3);

                    // position = abs + sign + floor + ceiling + log + log2 + log10 + exp + pow + round + sqrt;
                    position_0 = Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(abs_0, sign_0), floor_0), ceiling_0), log_0), log2_0), log10_0), exp_0), pow_0), round_0), sqrt_0);
                    position_1 = Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(abs_1, sign_1), floor_1), ceiling_1), log_1), log2_1), log10_1), exp_1), pow_1), round_1), sqrt_1);
                    position_2 = Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(abs_2, sign_2), floor_2), ceiling_2), log_2), log2_2), log10_2), exp_2), pow_2), round_2), sqrt_2);
                    position_3 = Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(Avx.Add(abs_3, sign_3), floor_3), ceiling_3), log_3), log2_3), log10_3), exp_3), pow_3), round_3), sqrt_3);

                    // --- 3. Store
                    Avx.Store(position_ptr +  0, position_0);
                    Avx.Store(position_ptr +  8, position_1);
                    Avx.Store(position_ptr + 16, position_2);
                    Avx.Store(position_ptr + 24, position_3);

                    position_ptr += 32;
                    velocity_ptr += 32;
                }
            }
            return i;
        }

    [SkipLocalsInit]
    private static void _Kernel_Misc_GPU(
        in GpuBuffers      buffers,
        in Buffer  <float> position_,
        in InBuffer<float> velocity_,
        in float           value)
    {
        var device         = (WgpuDevice)buffers.device;
        using var recorder = device.Recorder;

        var position    = recorder.RequireReadWrite(position_);
        var velocity    = recorder.RequireRead     (velocity_);

        // Recording - recorder provides Encoder
        var encoder = recorder.GetEncoder("Kernel_Misc"u8);
        using (var pass = encoder.BeginComputePass(_Kernel_Misc_GPU_EffectSlot, "Kernel_Misc"u8))
        {
            ref var effect = ref device.GetEffect(_Kernel_Misc_GPU_EffectSlot); // simple GpuEffect[] array lookup
            if (!effect.IsCreated) {
                effect = ref _Kernel_Misc_GPU_CreateEffect(device);
            }
            pass.SetPipeline(effect.pipeline);
            
            // Creation of buffer bind group is expensive. Try get from cache with two entries.
            var bufferGroup = effect.bufferCache.GetGroup(buffers.hash);
            if (!bufferGroup.IsCreated) {
                Span<BindGroupEntry> entries = stackalloc BindGroupEntry[2];
                entries[0] = WgpuBindGroup.From(0, position);
                entries[1] = WgpuBindGroup.From(1, velocity);
                bufferGroup = recorder.CreateBindGroup(effect.bufferLayout, entries, "Kernel_Misc_buffers"u8);
                device.UpdateBufferCache(_Kernel_Misc_GPU_EffectSlot, bufferGroup, buffers.hash);
            }
            pass.SetBindGroup(0, bufferGroup);
            
            var uniforms = new _Kernel_Misc_GPU_Uniforms {
                count           = buffers.length,
                position_off    = position_.Offset,
                velocity_off    = velocity_.Offset,
                value           = value,
            };
            var entry = recorder.AsUniformEntry(0, uniforms);
            // Creation of uniform bind group is cheap => no caching.
            var uniformGroup = recorder.CreateBindGroup(effect.uniformLayout, entry, "Kernel_Misc_uniforms"u8);
            pass.SetBindGroup(1, uniformGroup);
            
            pass.DispatchWorkgroups((buffers.length + 63) / 64, 1, 1);
            pass.End();
        }
        recorder.TrackWrite(position_);

        recorder.Finish(encoder, "Kernel_Misc"u8);

        // output.WaitInDebug();
    }
    
    [StructLayout(LayoutKind.Explicit, Size = 16)]  // WGSL layout: std140/std430
    private struct _Kernel_Misc_GPU_Uniforms
    {
        [FieldOffset( 0)]    public int        count;
        [FieldOffset( 4)]    public int        position_off;
        [FieldOffset( 8)]    public int        velocity_off;
        [FieldOffset(12)]    public float      value;
    }
    
    private static readonly int _Kernel_Misc_GPU_EffectSlot         = WgpuDevice.NewEffectSlot();
    private const ulong         _Kernel_Misc_GPU_BufferLayoutKey    = 0x332c677f8f18f451;
    private const ulong         _Kernel_Misc_GPU_UniformLayoutKey   = 0xeab614e96837d407;

    [MethodImpl(MethodImplOptions.NoInlining)]
    private static ref WgpuEffect _Kernel_Misc_GPU_CreateEffect(WgpuDevice device)
    {
        // @group(0)
        var bufferLayout = device.GetBindGroupLayout(_Kernel_Misc_GPU_BufferLayoutKey);
        if (!bufferLayout.IsCreated) {
            Span<WgpuLayoutEntry> buffers = stackalloc WgpuLayoutEntry[2];
            buffers[0] = WgpuLayoutEntry.ReadWriteStorage(0); // var<storage, read_write>  position_arr: array<f32>;
            buffers[1] = WgpuLayoutEntry.ReadOnlyStorage (1); // var<storage, read      >  velocity_arr: array<f32>;
            bufferLayout = device.CreateBindGroupLayout(buffers, _Kernel_Misc_GPU_BufferLayoutKey, "Kernel_Misc_buffers"u8);
        }
        // @group(1)
        var uniformLayout = device.GetBindGroupLayout(_Kernel_Misc_GPU_UniformLayoutKey);
        if (!uniformLayout.IsCreated) {
            Span<WgpuLayoutEntry> uniform = stackalloc WgpuLayoutEntry[1];
            uniform[0]    = WgpuLayoutEntry.Uniform(0); // var<uniform>              uniforms
            uniformLayout = device.CreateBindGroupLayout(uniform, _Kernel_Misc_GPU_UniformLayoutKey, "Kernel_Misc_uniforms"u8);
        }
        var shaderModule    = device.CreateShaderModule(_Kernel_Misc_GPU_Shader(), "Kernel_Misc"u8);
        var pipeline        = device.CreateComputePipeline(shaderModule, bufferLayout, uniformLayout, "Kernel_Misc"u8);
        
        return ref device.CreateEffect(_Kernel_Misc_GPU_EffectSlot, pipeline, bufferLayout, uniformLayout);
    }

    private static ReadOnlySpan<byte> _Kernel_Misc_GPU_Shader() =>
    """
    fn log10(x: f32) -> f32 { return log(x) / 2.3025851; }

    struct Kernel_Misc_Uniforms {
        count           : u32,           // offset:  0 size:  4
        position_off    : u32,           // offset:  4 size:  4
        velocity_off    : u32,           // offset:  8 size:  4
        value           : f32,           // offset: 12 size:  4
    };                                   //            size: 16
    
    @group(0) @binding(0) var<storage, read_write>  position_arr: array<f32>;
    @group(0) @binding(1) var<storage, read      >  velocity_arr: array<f32>;

    @group(1) @binding(0) var<uniform>              uniforms: Kernel_Misc_Uniforms;

    @compute @workgroup_size(64)
    fn Kernel_Misc(@builtin(global_invocation_id) global_id: vec3<u32>)
    {
        let index = global_id.x;
        if (index >= uniforms.count) {
            return;
        }
        var _position = position_arr[uniforms.position_off + index];
        var _velocity = velocity_arr[uniforms.velocity_off + index];
        var _value = uniforms.value;

        var _abs = abs(_velocity);
        var _sign = sign(_velocity);
        var _floor = floor(_velocity);
        var _ceiling = ceil(_velocity);
        var _log = log(_value);
        var _log2 = log2(_value);
        var _log10 = log10(_abs);
        var _exp = exp(_velocity);
        var _pow = pow(_abs, _velocity);
        var _round = round(_velocity);
        var _sqrt = sqrt(_abs);
        _position = ((((((((((_abs + _sign) + _floor) + _ceiling) + _log) + _log2) + _log10) + _exp) + _pow) + _round) + _sqrt);

        position_arr[uniforms.position_off + index] = _position;
    }
    """u8;

    #endregion
    }
}
