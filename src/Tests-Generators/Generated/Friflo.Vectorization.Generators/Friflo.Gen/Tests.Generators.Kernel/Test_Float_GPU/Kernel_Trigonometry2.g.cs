// <auto-generated/>
using System;
using System.ComponentModel;
using System.Numerics;
using System.Runtime.CompilerServices;
using System.Runtime.InteropServices;
using System.Runtime.Intrinsics;
using System.Runtime.Intrinsics.X86;
using Friflo.Vectorization.Intrinsics;
using Friflo.Vectorization.GPU;
using Friflo.Vectorization.GPU.Runtime;
using Friflo.Vectorization.WebGPU;
using Friflo.Vectorization.WebGPU.Runtime;


namespace Tests.Generators.Kernel
{
    public partial class Test_Float_GPU
    {
        /// <summary>Kernel method generated by: <see cref="Kernel_Trigonometry2"/>.</summary>
        public static GpuBuffer<float> Kernel_Trigonometry2Kernel(
            Buffer<float> position)
        {
            GpuBuffers buffers = new(position, nameof(position));

            if (!buffers.areSpans) {
                return _Kernel_Trigonometry2_GPU(buffers, position.gpuBuffer);
            }
            Kernel_Trigonometry2Vector(position.span);
            return null;
        }

        /// <summary>Vector method generated for: <see cref="Kernel_Trigonometry2"/>.</summary>
        public static void Kernel_Trigonometry2Vector(Span<float> position, bool vectorized = true)
        {
            int count = position.Length;
            int n = 0;
            if (vectorized) {
                if (Avx.IsSupported) {
                    n = _Kernel_Trigonometry2_Avx(count, position);
                }
            }
            for (; n < count; n++) {
                Kernel_Trigonometry2(ref position[n]);
            }
        }

    #region private members

        // [Layout: AoS-Vertical]  - lane-native speed
        [SkipLocalsInit]
        private static unsafe int _Kernel_Trigonometry2_Avx(int count,
            Span<float> position)
        {
            int i = 0;
            count -= 32;
            if (i > count) {
                return 0;
            }
            if (position.Length < count) VectorUtils.ThrowBufferTooSmall(nameof(position));

            fixed (float* position_first = position)
            {
                float* position_ptr = (float*)position_first;

                for (; i <= count; i += 32)
                {
                    // --- 1. Load
                    Vector256<float> position_0 = Avx.LoadVector256(position_ptr +  0);  // Single
                    Vector256<float> position_1 = Avx.LoadVector256(position_ptr +  8);  // Single
                    Vector256<float> position_2 = Avx.LoadVector256(position_ptr + 16);  // Single
                    Vector256<float> position_3 = Avx.LoadVector256(position_ptr + 24);  // Single

                    // --- 2. Compute
                    // var sinh = MathF.Sinh(position);
                    var sinh_0 = MathUtils.SinhMathF(position_0);
                    var sinh_1 = MathUtils.SinhMathF(position_1);
                    var sinh_2 = MathUtils.SinhMathF(position_2);
                    var sinh_3 = MathUtils.SinhMathF(position_3);

                    // var cosh = MathF.Cosh(position);
                    var cosh_0 = MathUtils.CoshMathF(position_0);
                    var cosh_1 = MathUtils.CoshMathF(position_1);
                    var cosh_2 = MathUtils.CoshMathF(position_2);
                    var cosh_3 = MathUtils.CoshMathF(position_3);

                    // var tanh = MathF.Tanh(position);
                    var tanh_0 = MathUtils.TanhMathF(position_0);
                    var tanh_1 = MathUtils.TanhMathF(position_1);
                    var tanh_2 = MathUtils.TanhMathF(position_2);
                    var tanh_3 = MathUtils.TanhMathF(position_3);

                    // position += sinh + cosh + tanh;
                    position_0 = Avx.Add(position_0, Avx.Add(Avx.Add(sinh_0, cosh_0), tanh_0));
                    position_1 = Avx.Add(position_1, Avx.Add(Avx.Add(sinh_1, cosh_1), tanh_1));
                    position_2 = Avx.Add(position_2, Avx.Add(Avx.Add(sinh_2, cosh_2), tanh_2));
                    position_3 = Avx.Add(position_3, Avx.Add(Avx.Add(sinh_3, cosh_3), tanh_3));

                    // --- 3. Store
                    Avx.Store(position_ptr +  0, position_0);
                    Avx.Store(position_ptr +  8, position_1);
                    Avx.Store(position_ptr + 16, position_2);
                    Avx.Store(position_ptr + 24, position_3);

                    position_ptr += 32;
                }
            }
            return i;
        }

    [SkipLocalsInit]
    private static GpuBuffer<float> _Kernel_Trigonometry2_GPU(
        in GpuBuffers buffers,
        GpuBuffer<float> position)
    {
        var device      = (WgpuDevice)buffers.GetDevice();
        // output ??= device.RentBuffer<float>(buffers.count);  TODO
        using var task  = device.RentTask();

        // Dependencies from inputs (out not Output!)

        // Recording - task provides Encoder
        var encoder = task.GetEncoder("Kernel_Trigonometry2"u8);
        using (var pass = encoder.BeginComputePass("Kernel_Trigonometry2"u8))
        {
            var effect = device.GetEffect(_Kernel_Trigonometry2_GPU_EffectSlot); // simple GpuEffect[] array lookup
            if (!effect.IsCreated) {
                effect = _Kernel_Trigonometry2_GPU_CreateEffect(device);
            }
            pass.SetPipeline(effect.pipeline);
            
            // Creation of buffer bind group is expensive. Try get from cache with two entries.
            var bufferGroup = effect.bufferCache.GetGroup(buffers.hash);
            if (!bufferGroup.IsCreated) {
                Span<BindGroupEntry> entries = stackalloc BindGroupEntry[1];
                entries[0] = WgpuBindGroup.From(0, position);
                bufferGroup = task.CreateBindGroup(effect.bufferLayout, entries, "Kernel_Trigonometry2_buffers"u8);
                device.UpdateBufferCache(_Kernel_Trigonometry2_GPU_EffectSlot, bufferGroup, buffers.hash);
            }
            pass.SetBindGroup(0, bufferGroup);
            
            var uniforms = new _Kernel_Trigonometry2_GPU_Uniforms {
                count = buffers.count,
            };
            var entry = task.AsUniformEntry(0, uniforms);
            // Creation of uniform bind group is cheap => no caching.
            var uniformGroup = task.CreateBindGroup(effect.uniformLayout, entry, "Kernel_Trigonometry2_uniforms"u8);
            pass.SetBindGroup(1, uniformGroup);
            
            pass.DispatchWorkgroups((buffers.count + 63) / 64, 1, 1);
            pass.End();
        }
        // connect task to output
        ((WgpuBuffer<float>)position).SetLastWritingTask(task);

        task.Finish(encoder, "Kernel_Trigonometry2"u8);
        device.Enqueue(task); // queues CommandBuffer only. No Submit().

        // output.WaitInDebug();
        return null;
    }
    
    [StructLayout(LayoutKind.Explicit, Size = 16)]  // WGSL layout: std140/std430
    private struct _Kernel_Trigonometry2_GPU_Uniforms
    {
        [FieldOffset(0)]    public int        count;
    }
    
    private static readonly int _Kernel_Trigonometry2_GPU_EffectSlot         = WgpuDevice.NewEffectSlot();
    private const ulong         _Kernel_Trigonometry2_GPU_BufferLayoutKey    = 0x8328507b4eb6ad4;
    private const ulong         _Kernel_Trigonometry2_GPU_UniformLayoutKey   = 0xeab614e96837d407;

    [MethodImpl(MethodImplOptions.NoInlining)]
    private static WgpuEffect _Kernel_Trigonometry2_GPU_CreateEffect(WgpuDevice device)
    {
        // @group(0)
        var bufferLayout = device.GetBindGroupLayout(_Kernel_Trigonometry2_GPU_BufferLayoutKey);
        if (!bufferLayout.IsCreated) {
            Span<WgpuLayoutEntry> buffers = stackalloc WgpuLayoutEntry[1];
            buffers[0] = WgpuLayoutEntry.ReadWriteStorage<float> (0); // var<storage, read_write>  position_arr: array<f32>;
            bufferLayout = device.CreateBindGroupLayout(buffers, _Kernel_Trigonometry2_GPU_BufferLayoutKey, "Kernel_Trigonometry2_buffers"u8);
        }
        // @group(1)
        var uniformLayout = device.GetBindGroupLayout(_Kernel_Trigonometry2_GPU_UniformLayoutKey);
        if (!uniformLayout.IsCreated) {
            Span<WgpuLayoutEntry> uniform = stackalloc WgpuLayoutEntry[1];
            uniform[0]    = WgpuLayoutEntry.Uniform<_Kernel_Trigonometry2_GPU_Uniforms> (0); // var<uniform>              uniforms
            uniformLayout = device.CreateBindGroupLayout(uniform, _Kernel_Trigonometry2_GPU_UniformLayoutKey, "Kernel_Trigonometry2_uniforms"u8);
        }
        var shaderModule    = device.CreateShaderModule(_Kernel_Trigonometry2_GPU_Shader(), "Kernel_Trigonometry2"u8);
        var pipeline        = device.CreateComputePipeline(shaderModule, bufferLayout, uniformLayout, "Kernel_Trigonometry2"u8);
        
        return device.CreateEffect(_Kernel_Trigonometry2_GPU_EffectSlot, pipeline, bufferLayout, uniformLayout);
    }

    private static ReadOnlySpan<byte> _Kernel_Trigonometry2_GPU_Shader() =>
    """
    struct Kernel_Trigonometry2_Uniforms {
        count      : u32,
    };
    
    @group(0) @binding(0) var<storage, read_write>  position_arr: array<f32>;

    @group(1) @binding(0) var<uniform>              uniforms: Kernel_Trigonometry2_Uniforms;

    @compute @workgroup_size(64)
    fn Kernel_Trigonometry2(@builtin(global_invocation_id) global_id: vec3<u32>)
    {
        let index = global_id.x;
        if (index >= uniforms.count) {
            return;
        }
        var position = position_arr[index];

        var sinh = sinh(position);
        var cosh = cosh(position);
        var tanh = tanh(position);
        position += ((sinh + cosh) + tanh);

        position_arr[index] = position;
    }
    """u8;

    #endregion
    }
}
